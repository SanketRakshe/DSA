#include<iostream>
using namespace std;

int Sum(int n) {
    if(n == 0)          //base case 
    return 0;

    return n + Sum(n-1);
}
int main() {
    int n;
    cout<<"Enter Number: ";
    cin>>n;

    int Addition = Sum(n);
    cout<<"Addition is: "<< Addition <<endl;

    return 0;
}
